# PRODUCT AUDIT 1 — El Bingo del Cargador — 2026-08-23
Scope: whole product (code, docs tree, roadmap, fix index, forge, workflow artifacts)
Coverage: dimensions run statically — build gate executed live; forge facts verified live via `gh` (2026-08-23); security = code inspection of auth/rate-limit/turnstile/secrets surfaces (sampled key endpoints, not a pentest); UX/a11y/SEO = doc + metadata level only (no browser/pixel pass — Claude Preview MCP not available in this session); performance = static analysis only (project has no bench tooling by design)
Verdict: Functionally healthy product with a green strict build and a solid auth/rate-limit posture, but process artifacts have drifted badly — the fix index is entirely stale, the capability inventory denies the shipped auth subsystem — and CLAUDE.md's "no brand names" hard rule is contradicted by shipped, separately-documented UI.

## Health by dimension
  Correctness & architecture ... ✓ healthy (build green under astro/tsconfigs/strict; flat arch intact; settleDeparture discipline held; invariants doc present)
  Security ..................... ⚠ concerns (solid: HttpOnly/Secure/SameSite session cookie `src/lib/auth.ts:152-159`, OAuth state-CSRF check `callback.ts:50-54`, Turnstile on create/recover/join, rate-limit bindings on all write routes; concern: stray supply-chain deps, F4)
  Performance .................. ⚠ concerns (all known hotspots already tracked: #16 profile LIMIT, #11 edge cache, #49 waitUntil; no bench tooling exists to measure anything)
  Tests ........................ n-a (no test suite by design — gate is `npm run build`; consequence tracked in F5)
  UX / UI ...................... ✓ healthy (design system shipped in feature 14; static review only — no browser pass this run)
  Accessibility ................ ⚠ concerns (#18 dialog aria-labelledby defect open; ACCESSIBILITY.md maintained)
  SEO .......................... ✓ healthy (OG PNG pipeline fixed via #68–#77; minor follow-ups #65/#66 open)
  Brand / voice ................ ✗ at risk (CLAUDE.md no-brand hard rule vs shipped Tesla referral CTA + brand selector — F3)
  Tech debt .................... ✓ healthy (near-zero TODO/FIXME; single justified @ts-ignore `svg-to-png.ts:3`; extraction fixes landed via #56/#57)
  Process & docs ............... ✗ at risk (fix index fully stale F1; CAPABILITIES.md auth drift F2; manual backlog aging F5)
  Workflow discipline .......... ⚠ concerns (unticked tasks in 7 done units F7; bare `done` rows without PR links F8; junk commit messages F9; documented slice-naming exception F10)
  Roadmap coherence ............ ⚠ concerns (rows 06/07 mislabeled `planned` with no artifacts F6; otherwise coherent, deps consistent)
  Installed tooling ............ ⚠ concerns (CLAUDE.md companion-review names don't match the installed review pack)

## Findings (severity-ranked, one F-sequence for the whole audit)
  F1 [high] Process & docs — process-fix-index-stale — every one of the 17 rows in the Active fixes table references a CLOSED issue and there are zero open PRs: `pending` row `78-dependency-upgrade` closed via merged PR #80 (2026-08-21); `done` rows 71/72 were never removed after merges #75/#73 per the index's own convention ("Remove the row when the PR merges"); all 14 `in-review` rows merged weeks ago (PRs #53–#61, #68–#70) — evidence: docs/fix/README.md table vs `gh issue view`/`gh pr list` live state 2026-08-23 — class: fix-now
  F2 [high] Process & docs — process-capabilities-auth-drift — docs/CAPABILITIES.md declares Authentication `no` ("No accounts or auth") and omits any authenticated-account role, while feature 05 `accounts` is done: Google/X OAuth, `sessions`/`oauth_state` tables, live routes `src/pages/api/auth/[provider]/{start,callback}.ts`, `logout.ts`, account-link/delete/profile endpoints; design-feature's Integration closure walks this file, so future features will be designed believing no auth subsystem exists — evidence: docs/CAPABILITIES.md Roles + subsystems table vs src/pages/api/auth/ and migrations/0011_accounts.sql — class: fix-now
  F3 [med] Brand / voice — brand-referral-convention-conflict — CLAUDE.md hard rule says "**No brand names anywhere** (no charger networks, no car makers)", yet shipped UI carries a personal Tesla referral CTA (`https://www.tesla.com/es_es/referral/gabriel746720`, `index.astro:390-416`), "#Tesla" share hashtags (`index.astro:1344`, `g/[id].astro:682`), and a 14-brand vehicle selector (`src/lib/card.ts:24`, `display.ts:2`) — the CTA and selector are deliberately documented in `docs/frontend/COPYWRITING.md:32`, `DESIGN.md:56`, `docs/legal/README.md:72`, so the contradiction is CLAUDE.md vs three other governed docs; an agent honoring CLAUDE.md would try to strip sanctioned UI — needs an explicit convention amendment (scoped exception) or removal decision — class: tradeoff
  F4 [med] Security — security-stray-tooling-deps — four production dependencies are imported nowhere in `src/`: `@joemccann/pi-pdf@^1.0.1` and `pi-subagents@^0.49.0` (agent-harness packages), plus bare `version@^0.1.2` and `view@^1.1.1` (likely accidental installs); all outside the closed runtime-dependency catalog (hard convention; REPOSITORY_STATE OQ-001 open since 2026-08-20) — pure install/supply-chain surface in a Worker app — evidence: package.json dependencies vs grep of src/ (no matches) + `npm ls` — class: fix-now
  F5 [med] Process & docs — process-manual-backlog-aging — with no test suite, the only behavioral verification is manual, and the dedicated backlog issue #35 has sat open since 2026-06-21 while ~10 more fixes/features merged; meanwhile 7 done features still hold unticked manual-check boxes (see F7) — verification debt is accumulating silently — evidence: `gh issue view 35` (open, 2026-06-21) + unticked TASKS files below — class: postpone
  F6 [low] Roadmap coherence — roadmap-planned-without-artifacts — roadmap rows 06 `achievements-badges` and 07 `situations-total-count` read `planned`, but no `docs/features/06-*`/`07-*` folders exist; per the five-state machine `planned` requires SPEC + planning artifacts — these rows are effectively `idea` and sensors/executors will misroute them — evidence: ROADMAP.md rows 06/07 vs `find docs/features` — class: fix-now
  F7 [low] Workflow discipline — workflow-done-tasks-unticked — done features 01, 03, 05, 09, 12, 14, 15 all contain unchecked `- [ ]` items in TASKS.md, including manual visual/behavioral checks (audit-docs check 11) — likely done-but-unrecorded rather than undone, but the artifacts no longer distinguish — evidence: e.g. docs/features/01-final-certificate-design/TASKS.md:9-112 — class: postpone
  F8 [low] Workflow discipline — workflow-done-rows-no-pr-links — every `done` roadmap row is bare `done` with no `done · [#<pr>]` link (audit-docs check 9): merge state must be re-derived from the forge each time (e.g. 16→#64, 15→#58 had to be looked up manually) — evidence: ROADMAP.md Features table — class: postpone
  F9 [low] Workflow discipline — workflow-commit-format — nonconforming subjects on main: `503b430 chore: update?` (no scope, junk summary) and two unscoped `b440d25`/`302c987 chore: upgrade skills` — violates `<type>(<scope>): <summary>` — evidence: git log --first-parent — class: postpone
  F10 [low] Workflow discipline — workflow-phase-naming-slices — feature 14 PLAN/TASKS use slice ids S1–S8 instead of P-phases (audit-docs check 10 hit) — recorded deliberate exception (SPEC decision D10: "8 independently-shippable slices"), so informational; do not rename retroactively — evidence: docs/features/14-design-refactor-design-system/PLAN.md:11-23 — class: tradeoff

## Proposals — the user decides which to act on

### Issues to open
  - Purge stale fix index (remove/update all 17 rows to match forge truth; adopt post-merge removal habit) [high] — from: F1 — the index is consumed as a worklist by sensors and planning; today it lies about 17 units — route: /triage-issue 1 F1 — evidence: docs/fix/README.md vs gh live state
  - Refresh CAPABILITIES.md (Authentication yes/partial, add authenticated-account role, cite auth routes) [high] — from: F2 — keeps Integration closure honest for every future design — route: /triage-issue 1 F2 — evidence: docs/CAPABILITIES.md vs src/pages/api/auth/
  - Remove stray tooling deps (`@joemccann/pi-pdf`, `pi-subagents`, `version`, `view`) from package.json [med] — from: F4 — closes OQ-001, restores the closed dependency catalog — route: /triage-issue 1 F4 (then /plan-fix) — evidence: package.json; npm ls
  - Reconcile the no-brand hard rule with shipped brand surfaces (amend CLAUDE.md wording with the sanctioned referral CTA + vehicle-selector exception, or decide removal) [med] — from: F3 — one governed sentence ends the contradiction — route: /triage-issue 1 F3 — evidence: CLAUDE.md hard conventions vs docs/frontend/{COPYWRITING,DESIGN}.md
  - Run the manual verification backlog session and close/refresh #35 [med] — from: F5 — converts silent verification debt into recorded outcomes — route: /triage-issue 35 (existing issue, classify + schedule) — evidence: gh issue view 35

### Roadmap — add
  (none — the existing planned set already covers what this sweep's evidence justifies: 17-server-side-account removes a real per-load 401 wart, 07 is an XS quick win feeding 06; nothing new surfaced that outweighs finishing those)

### Roadmap — remove / revise
  - Revise rows 06/07 status from `planned` to `idea` (or author their SPECs if they truly are planned) — from: F6 — aligns the table with the five-state machine so execute-phase routing stops lying — route: roadmap edit / /triage-issue 1 F6

### Tooling — register / re-design
  - Correct CLAUDE.md companion-review skill names to the installed pack: `review-code`, `review-security`, `review-verify`, `review-debt`, `review-design`, `review-a11y`, `review-brand`, `review-perf`, `review-seo` (today it names `code-review`, `security-review`, `verify`, `tech-debt` which don't exist as installed) — route: user edits CLAUDE.md
  - Register Serena MCP (LSP symbol navigation/editing) as an optional dev extra — useful for the large `index.astro` refactor that feature 17 will perform — route: user edits CLAUDE.md
  - Claude Preview MCP was not connected in the session that ran this audit — if that remains true in normal sessions, the manual-verification harness named in CLAUDE.md is unavailable; re-register or substitute — route: user edits CLAUDE.md MCP servers table

## Manual-verification checklist (what automation can't confirm)
  - Exercise the core play flows in a browser (`npm run dev`): card create → marks → complete → diploma → group join/leave/kick — the gate is build-only and nothing here ran the app
  - In production, fetch `/og/diploma/<completed-id>.png` and confirm Lora renders (feature 14 watch-list: silent Georgia fallback if CF rasterizer ignores embedded @font-face)
  - Check home OG title/image in square-crop previews (#65) and confirm og:image:alt follow-up scope (#66)
  - Confirm remote D1 is at migration 0015 before the next `npm run deploy` (deploy applies idempotently, but verify)

Finding set: F1 + F2 + F3 + F4 + F5 + F6 + F7 + F8 + F9 + F10
→ Next: /triage-issue 1 F1 F2 F3 F4 F6 — classify the proposed issue set in one batch (opens the ones that warrant it)
  · accepted bug/debt → /plan-fix   · accepted capability → /plan-feature
  · nothing to act on → the persisted report is the record; move on
